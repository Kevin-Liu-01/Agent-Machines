node_modules
.next
out
.env
.env.local
*.log
.DS_Store
.vercel
.turbo
next-env.d.ts
tsconfig.tsbuildinfo

# clerk configuration (can include secrets)
/.clerk/

# Local dev store -- file-backed UserConfig for `ALLOW_DEV_AUTH=1`.
# Holds provider keys and machine refs for the synthetic dev-user.
.dev-user-config.json

# This is a pnpm project (see `packageManager` in package.json). Never commit an
# npm lockfile: a stale package-lock.json alongside pnpm-lock.yaml caused a
# Vercel resolution mismatch (e2b's chalk resolved differently). pnpm only.
package-lock.json
