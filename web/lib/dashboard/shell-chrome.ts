/** Both sides of the dashboard share one fixed-height, single-line header. */
export const DASHBOARD_SHELL_HEADER_ROW =
	"flex h-12 min-h-12 max-h-12 shrink-0 flex-nowrap items-center border-b border-[var(--ret-border)]";
